interface:
  display_name: "MaxVideoAI Plan"
  short_description: "Compare models, budget projects, and plan shots"
  default_prompt: "Use $plan to turn my brief into a shot list, model comparison, and project budget."
dependencies:
  tools:
    - type: "mcp"
      value: "maxvideoai"
      description: "Live MaxVideoAI model, capability, and project budget tools"
      transport: "streamable_http"
      url: "https://api.maxvideoai.com/mcp"
policy:
  allow_implicit_invocation: true
